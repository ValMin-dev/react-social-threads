// Подключаем глобальные типы Vitest, чтобы тесты понимали describe, it, expect и другие команды.
/// <reference types="vitest/globals" />

// Добавляем удобные проверки для тестов, например toBeInTheDocument().
import "@testing-library/jest-dom"
