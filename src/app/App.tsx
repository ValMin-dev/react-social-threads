import { Button } from "@nextui-org/react"

// Это простой тестовый компонент с одной кнопкой.
const App = () => {
  return (
    <div>
      {/* Просто кнопка для проверки, что UI-библиотека работает. */}
      <Button color="primary">Test</Button>
    </div>
  )
}

// Делаем компонент доступным для импорта в других файлах.
export default App
